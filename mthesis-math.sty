\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mthesis-math}

\usepackage{import}
% Margins for thesis template

%--- Symmetric Margins
\usepackage[a4paper,left=2.5cm,top=2.3cm,right=2.5cm,bottom=2cm]{geometry}

%--- Asymmetric Margins
% \usepackage[a4paper,inner=2.7cm,top=2.3cm,outer=2.0cm,bottom=2cm]{geometry}

% Header
\usepackage{fancyhdr}
\pagestyle{fancy}

\fancyhead[LO]{}
\fancyhead[RE]{}
\fancyhead[RO]{\slshape \rightmark}
\fancyhead[LE]{\slshape \leftmark}
\fancyfoot[C]{\thepage}
\setlength{\headheight}{15pt}
% \fancyhead[LE,RO]{\thepage}
% \fancyhead[RE]{\textbf{\textit{\nouppercase{\leftmark}}}}
% \fancyhead[LO]{\textbf{\textit{\nouppercase{\rightmark}}}}
\fancypagestyle{plain}{ %
\fancyhf{} % remove everything
\renewcommand{\headrulewidth}{0pt} % remove lines as well
\renewcommand{\footrulewidth}{0pt}}

\newcommand\frontmatter{%
	\cleardoublepage
	\pagenumbering{roman}
}

\newcommand\mainmatter{%
	\cleardoublepage
	\pagenumbering{arabic}
}

\newcommand\backmatter{%
	\if@openright
		\cleardoublepage
	\else
		\clearpage
	\fi
}

% Importing graphics
\usepackage{graphicx}
\graphicspath{ {img/} }

% Tikz with externalized enabled (Highly recommended if you are using a lot of tikz)
\usepackage{tikz}
% To disable externalize, comment out these 3 lines
\usetikzlibrary{external}
\tikzexternalize[prefix=tikz/]

\usepackage{mthesis-user}
\usepackage{mthesis-img}

% Fix the bug in latex that makes mathmode in bold ugly.
\g@addto@macro\bfseries{\boldmath}